import * as seed from "@/mock/data";

/**
 * Mutable in-memory store so create/update/delete feel real in mock mode.
 * Reset on full page reload.
 */
export const mockStore = {
  users: [...seed.users],
  projects: [...seed.projects],
  tasks: [...seed.tasks],
  bugs: [...seed.bugs],
  sprints: [...seed.sprints],
  timeEntries: [...seed.timeEntries],
  projectMembers: [...seed.projectMembers],
  projectActivity: [...seed.projectActivity],
  milestones: [...seed.milestones],
  documents: [...seed.documents],
  pipelineStages: [...seed.pipelineStages],
  companies: [...seed.companies],
  contacts: [...seed.contacts],
  leads: [...seed.leads],
  deals: [...seed.deals],
  followUps: [...seed.followUps],
  employees: [...seed.employees],
  attendance: [...seed.attendance],
  leaves: [...seed.leaves],
  holidays: [...seed.holidays],
  performanceReviews: [...seed.performanceReviews],
  jobPostings: [...seed.jobPostings],
  jobApplications: [...seed.jobApplications],
  salarySlips: [...seed.salarySlips],
  invoices: [...seed.invoices],
  quotations: [...seed.quotations],
  expenses: [...seed.expenses],
  subscriptions: [...seed.subscriptions],
  contracts: [...seed.contracts],
  tickets: [...seed.tickets],
  meetings: [...seed.meetings],
  categories: [...seed.categories],
  blogPosts: [...seed.blogPosts],
  media: [...seed.media],
  cmsPages: [...seed.cmsPages],
  apiKeys: [...seed.apiKeys],
  auditLogs: [...seed.auditLogs],
  notifications: [...seed.notifications],
  messages: [...seed.messages],
  payments: [...seed.payments],
  feedback: [...seed.feedback],
  permissions: [...seed.permissions],
  adminSettings: { ...seed.adminSettings },
};
